apiVersion: v1
kind: Service
metadata:
  name: {{ include "two-tier-chart.fullname" . }}
  labels:
    {{- include "two-tier-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort:  {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "two-tier-chart.selectorLabels" . | nindent 4 }}
