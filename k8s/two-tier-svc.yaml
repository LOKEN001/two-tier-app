apiVersion: v1
kind: Service
metadata:
   name: two-tier-svc
spec:
   selector:
      app: two-tier-server
   ports:
      - protocol: TCP
        port: 80
        targetPort: 5000
   type: NodePort     
